import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Patrick Azabe - Software Engineer',
  description: 'Software Engineer with expertise in backend development, mobile applications, and full-stack solutions. Specializing in Java, PHP, .NET, Ionic, Node.js, Angular, and React.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>{children}</body>
    </html>
  )
}

